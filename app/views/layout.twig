<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <link href="{{ asset('css/reset.css') }}" rel="stylesheet" type="text/css" />
        <link href="{{ asset('css/main.css') }}" rel="stylesheet" type="text/css" />
        <title>{% block title %}SillyCMS{% endblock %}</title>
    </head>
    <body>
        <div class="page">
            <div class="heading">
                <h1>{{ block('title') }}</h1>
            </div>
            <div class="nav">
                {% render "SillyCMSBundle:Page:index" %}
            </div>
            <div class="content">
                {% block body %}{% endblock %}
            </div>
            <div class="footer">
                <a href="https://github.com/boutell/SillyCMS">SillyCMS</a> is a <a href="http://www.symfony-reloaded.org/">Symfony 2</a> demo.
                {# Doesn't work, complains that ifrole is not available even though it's in the docs #}
                {# ifrole "IS_AUTHENTICATED_FULLY" #}
                    {# <a href="{{ _path('logout') }}">Logout</a> #}
                {# endifrole #}
            </div>
        </div>
    </body>
</html>
