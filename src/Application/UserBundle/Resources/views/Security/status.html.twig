{% if security.vote('IS_AUTHENTICATED_FULLY') %}
  Logged in as {{ user.username }} <a class="button" href="{{ path('_security_logout') }}">Log Out</a>
{% else %}
  <a class="button" href="{{ path('_security_login') }}">Log In</a>
{% endif %}
